# React Task List

React 18 + TypeScript + Fluent UI frontend for the Task Management API.

From the repository root:

```bash
npm install
npm run start:web
```

Open `http://localhost:5173`. Vite proxies `/api` requests to Azure Functions at `http://localhost:7071`.

Production build:

```bash
npm run build:web
```
